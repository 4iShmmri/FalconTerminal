# Build artifacts
.build/
build/
DerivedData/
# Note: the distributable FalconTerminal.dmg at the repo root is tracked
# intentionally so it sits next to the README.

# SwiftPM
Package.resolved

# macOS
.DS_Store
*.swp

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
